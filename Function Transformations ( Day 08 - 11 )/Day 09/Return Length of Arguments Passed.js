/**
 * Return the count of arguments passed to it.
 * @param {*} args - A variable number of arguments.
 * @returns {number} The count of arguments passed to the function.
 */
function argumentsLength(...args) {
  return args.length;
}
