
# 🧩 LeetCode 2724: Sort By

## 📘 সমস্যা বিবরণ (Problem Description)

তোমাকে একটি **array** এবং একটি **function fn** দেওয়া হয়েছে।  
তোমার কাজ হলো — **fn(element)** এর মান অনুযায়ী array–এর উপাদানগুলোকে **sort (ক্রম অনুযায়ী সাজানো)** করা।

অর্থাৎ, প্রতিটি element–এর জন্য `fn` ফাংশন যে মান রিটার্ন করে, সেটির উপর ভিত্তি করে array–টি সাজাতে হবে।

---

## 🧠 উদাহরণ (Examples)

```js
Example 1:

Input: arr = [5, 4, 1, 2, 3], fn = (x) => x
Output: [1, 2, 3, 4, 5]
Explanation: fn simply returns the number passed to it so the array is sorted in ascending order.
Example 2:

Input: arr = [{"x": 1}, {"x": 0}, {"x": -1}], fn = (d) => d.x
Output: [{"x": -1}, {"x": 0}, {"x": 1}]
Explanation: fn returns the value for the "x" key. So the array is sorted based on that value.
Example 3:

Input: arr = [[3, 4], [5, 2], [10, 1]], fn = (x) => x[1]
Output: [[10, 1], [5, 2], [3, 4]]
Explanation: arr is sorted in ascending order by number at index=1. 
 

Constraints:

arr is a valid JSON array
fn is a function that returns a number
1 <= arr.length <= 5 * 105
```

### 💡 ভাবনা (Intuition)

JavaScript–এর Array.prototype.sort() মেথড ব্যবহার করা যায়, যেখানে একটি custom comparator দেওয়া যায়।
আমরা প্রতিটি element–এর ওপর fn() চালিয়ে তার মান অনুযায়ী compare করব।
```js
arr.sort((a, b) => fn(a) - fn(b));
```

### 🧮 JavaScript সমাধান (Solution)
```js
/**
 * @param {Array} arr
 * @param {Function} fn
 * @return {Array}
 */
var sortBy = function(arr, fn) {
  return arr.sort((a, b) => {
    const valA = fn(a);
    const valB = fn(b);
    return valA - valB;
  });
};

```
### 🧠 ব্যাখ্যা (Explanation)
| ধাপ | কাজ                                                    | উদাহরণ                   |
| --- | ------------------------------------------------------ | ------------------------ |
| 1️⃣ | `sortBy` ফাংশন ইনপুট নেয় `arr` ও `fn`                  | `[5,4,3,2,1], x => x`    |
| 2️⃣ | `sort()` এর মধ্যে custom compare function ব্যবহার      | `(a,b) => fn(a) - fn(b)` |
| 3️⃣ | প্রতিটি element–এর fn মানের পার্থক্য অনুযায়ী সাজানো হয় | ascending order          |
| 4️⃣ | sort শেষে array return করা হয়                          | ✅                        |


🧾 উদাহরণসহ টেস্ট
```js
console.log(sortBy([5, 4, 1, 2, 3], x => x));
// Output: [1, 2, 3, 4, 5]

console.log(sortBy([{"x":1}, {"x":0}, {"x":-1}], d => d.x));
// Output: [{"x":-1},{"x":0},{"x":1}]

console.log(sortBy([[3], [1], [2]], x => x[0]));
// Output: [[1], [2], [3]]

```

⚡ উন্নত (One-Liner) সমাধান
```js
const sortBy = (arr, fn) => arr.sort((a, b) => fn(a) - fn(b));

```
🔍 যদি Descending (উল্টো) ক্রমে সাজাতে চাও
```js
const sortByDesc = (arr, fn) => arr.sort((a, b) => fn(b) - fn(a));

```








