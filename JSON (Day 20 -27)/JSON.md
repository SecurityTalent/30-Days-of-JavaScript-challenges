# 🧩 JSON (JavaScript Object Notation)

## 📖 JSON কী?

**JSON** হলো একটি **data format** (ডেটা ফরম্যাট), যা তথ্য (data) সংরক্ষণ ও বিনিময়ের জন্য ব্যবহৃত হয়।  
এটি খুবই হালকা, সহজবোধ্য এবং মানুষের পঠনযোগ্য।  
মূলত **JavaScript** ভাষা থেকে এর উৎপত্তি হলেও বর্তমানে প্রায় সব প্রোগ্রামিং ভাষাতেই JSON ব্যবহার করা যায়।

---

## 🧠 JSON-এর বৈশিষ্ট্য

- 🔹 কী–ভ্যালু (key–value) জোড়ায় তথ্য সংরক্ষণ করে  
- 🔹 `{}` ব্র্যাকেটের মধ্যে অবজেক্ট থাকে  
- 🔹 `[]` ব্র্যাকেটের মধ্যে অ্যারে থাকে  
- 🔹 টেক্সট (string) ডাবল কোট `" "` এর মধ্যে লেখা হয়  
- 🔹 JSON শুধুমাত্র **data** ধারণ করে, কোনো ফাংশন বা মেথড নয়  

---

## 🧱 JSON-এর গঠন (Structure)

```json
{
  "name": "Rahim",
  "age": 25,
  "isStudent": true,
  "skills": ["HTML", "CSS", "JavaScript"],
  "address": {
    "city": "Dhaka",
    "country": "Bangladesh"
  }
}
```

## 🧩 ব্যাখ্যা:

- "name" → কী (key)
- "Rahim" → ভ্যালু (value)
- "skills" → একটি অ্যারে (array)
- "address" → একটি nested অবজেক্ট

## ⚙️ JSON কোথায় ব্যবহৃত হয়?

- 🌐 API (Application Programming Interface) এ data আদান–প্রদানে
- 💾 Configuration file হিসেবে (যেমন: package.json, settings.json)
- 📡 Server ও client এর মধ্যে ডেটা পাঠানো ও গ্রহণে
- 🧰 Database (যেমন MongoDB) তে ডেটা সংরক্ষণে

## 🧮 JSON এবং JavaScript উদাহরণ
```JS
// JSON Data
const person = {
  "name": "Karim",
  "age": 30,
  "city": "Chattogram"
};

// JavaScript এ ব্যবহার
console.log(person.name);  // Output: Karim

```

## 🔄 JSON পার্স করা (Parse)

JSON string → JavaScript object এ রূপান্তর করতে:
```JS
const jsonString = '{"name":"Nadia","age":22}';
const obj = JSON.parse(jsonString);
console.log(obj.name); // Nadia

```
JavaScript object → JSON string এ রূপান্তর করতে:
```JS
const data = { name: "Nadia", age: 22 };
const jsonData = JSON.stringify(data);
console.log(jsonData); // {"name":"Nadia","age":22}

```

### ⚠️ JSON লেখার সময় সতর্কতা

- ❌ শেষে কমা দেওয়া যাবে না
- ❌ key অবশ্যই " " ডাবল কোটের মধ্যে থাকতে হবে
- ❌ comment লেখা যায় না

```js
{
  "title": "JSON Example",
  "author": "OpenAI",
  "tags": ["data", "format", "api"],
  "published": true
}
```
