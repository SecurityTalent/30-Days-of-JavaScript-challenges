
# 🧩 LeetCode 2727: Is Object Empty

## 📘 সমস্যা বিবরণ (Problem Description)

তোমাকে একটি **object** বা **array** দেওয়া হয়েছে।  
তোমার কাজ হলো নির্ধারণ করা যে সেটি **খালি (empty)** কিনা।

- একটি **empty object** মানে সেটিতে কোনো key–value pair নেই।  
- একটি **empty array** মানে সেটিতে কোনো element নেই।

তুমি ধরে নিতে পারো যে object বা array টি **`JSON.parse`** এর আউটপুট হিসেবে পাওয়া।

---
## 🧠 উদাহরণ (Examples)

### ✅ Example 1:
**Input:**
```js
obj = {"x": 5, "y": 42}

// Output: false
// Explanation: object–এ 2টি key আছে (x, y), তাই এটি খালি নয়।
```
### ✅ Example 2:
**Input:**
```js

Input: obj = {}

// Output: true
// Explanation: এখানে কোনো key–value pair নেই, তাই এটি খালি।
```

### ✅ Example 3:
**Input:**
```js
Input: obj = [null, false, 0]

// Output: false
// Explanation: array–এ 3টি element আছে, তাই এটি খালি নয়।
```

### 💡 ভাবনা (Intuition)

JavaScript–এ object এবং array উভয়ই typeof "object" রিটার্ন করে, তাই আমাদের genericভাবে check করতে হবে।

1️⃣ Array হলে — obj.length ব্যবহার করে দেখা যায় এটি খালি কিনা।
2️⃣ Object হলে — Object.keys(obj) ব্যবহার করে দেখা যায় কোনো key আছে কিনা।

### 🧮 JavaScript সমাধান (Solution)
```JS
/**
 * @param {Object|Array} obj
 * @return {boolean}
 */
var isEmpty = function(obj) {
  // যদি array হয়
  if (Array.isArray(obj)) {
    return obj.length === 0;
  }
  
  // যদি object হয়
  return Object.keys(obj).length === 0;
};

```
### 🧠 ব্যাখ্যা (Explanation)
| ধাপ | কাজ                                              | ফলাফল                  |
| --- | ------------------------------------------------ | ---------------------- |
| 1️⃣ | `Array.isArray(obj)` দিয়ে চেক করি এটি array কিনা | true বা false          |
| 2️⃣ | array হলে — `obj.length === 0` চেক               | খালি হলে true          |
| 3️⃣ | object হলে — `Object.keys(obj).length === 0` চেক | কোনো key না থাকলে true |
| 4️⃣ | অন্য সব ক্ষেত্রে false রিটার্ন                   | —                      |


### ⚡ O(1) টাইমে করা সম্ভব?

হ্যাঁ ✅
Object.keys(obj) বা obj.length অপারেশন সাধারণত constant time–এর মতো দ্রুত হয়
(যদিও বাস্তবে এটি O(n) হতে পারে), তবে ছোট JSON ডেটায় এটি O(1) equivalent ধরে নেওয়া যায়।

```JS
console.log(isEmpty({}));              // true
console.log(isEmpty({"x": 5}));        // false
console.log(isEmpty([]));              // true
console.log(isEmpty([null, 0]));       // false

```
