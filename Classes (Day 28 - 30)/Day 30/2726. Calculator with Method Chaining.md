# 🧩 LeetCode 2726: Calculator with Method Chaining

## 📘 সমস্যা বিবরণ (Problem Description)

তোমাকে একটি **Calculator class** তৈরি করতে হবে যা **method chaining** সমর্থন করবে।  

### Requirements:
- **constructor(value)** → initial value দিয়ে calculator তৈরি করবে।  
- **add(num)** → বর্তমান value–এর সাথে num যোগ করবে।  
- **subtract(num)** → বর্তমান value–এর থেকে num বিয়োগ করবে।  
- **multiply(num)** → বর্তমান value–এর সাথে num গুণ করবে।  
- **divide(num)** → বর্তমান value–কে num দ্বারা ভাগ করবে।  
- **getValue()** → বর্তমান value return করবে।  
- সব arithmetic method–এর পরে object return করতে হবে, যাতে **method chaining** কাজ করে।

---

## 💡 Intuition

1. Calculator class–এ একটি property রাখব, যেমন `this.value`.  
2. প্রতিটি arithmetic method update করবে `this.value`.  
3. প্রতিটি arithmetic method শেষে **`this` return করবে**, যাতে method chaining সম্ভব হয়।  
4. `getValue()` শুধু final value return করবে।  

---

## 🧮 JavaScript সমাধান

```javascript
class Calculator {
  constructor(value = 0) {
    this.value = value;
  }

  add(num) {
    this.value += num;
    return this;
  }

  subtract(num) {
    this.value -= num;
    return this;
  }

  multiply(num) {
    this.value *= num;
    return this;
  }

  divide(num) {
    this.value /= num;
    return this;
  }

  getValue() {
    return this.value;
  }
}
```
### 🧠 ব্যাখ্যা
| ধাপ | কাজ                                              | উদাহরণ                  |
| --- | ------------------------------------------------ | ----------------------- |
| 1️⃣ | `constructor(value)` → initial value save        | `value = 10`            |
| 2️⃣ | `add(num)` → value update এবং `this` return      | `calc.add(5)` → 15      |
| 3️⃣ | `subtract(num)` → value update এবং `this` return | `calc.subtract(3)` → 12 |
| 4️⃣ | `multiply(num)` → value update এবং `this` return | `calc.multiply(2)` → 24 |
| 5️⃣ | `divide(num)` → value update এবং `this` return   | `calc.divide(4)` → 6    |
| 6️⃣ | `getValue()` → final value return                | `6`                     |

### 🧾 উদাহরণ
```js
const calc = new Calculator(10);

const result = calc
  .add(5)        // 10 + 5 = 15
  .subtract(3)   // 15 - 3 = 12
  .multiply(2)   // 12 * 2 = 24
  .divide(4)     // 24 / 4 = 6
  .getValue();   // 6

console.log(result); // 6
```
Method chaining–এর কারণে আমরা এক লাইনে একাধিক operation করতে পারি।


🔹 Key Points
- Method chaining করার জন্য প্রতিটি arithmetic method–এ return this করতে হবে।
- getValue() শুধু শেষের value return করবে।
- এটা fluent interface pattern–এর একটি উদাহরণ।
