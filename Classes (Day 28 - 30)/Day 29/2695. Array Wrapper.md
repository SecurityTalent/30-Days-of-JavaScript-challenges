# 🧩 LeetCode 2695: Array Wrapper

## 📘 সমস্যা বিবরণ (Problem Description)

তোমাকে একটি **Array Wrapper class** তৈরি করতে হবে যা একটি array কে wrap করে এবং কিছু সুবিধা দেয়:

- **constructor(nums)** → array initialize করা।  
- **valueOf()** → object কে number হিসেবে convert করা যায়।  
- **toString()** → object কে string হিসেবে convert করা যায়।  

এছাড়া, array–এর element–এর সাথে operator ব্যবহার করলে wrapper class ঠিকভাবে behave করবে।  

---

## 💡 Intuition

1. Wrapper class–এ array store করা।  
2. `valueOf()` → array–এর sum return করবে, যাতে number operations ঠিক হয়।  
3. `toString()` → array–এর string representation return করবে।  

---

## 🧮 JavaScript সমাধান

```javascript
class ArrayWrapper {
  constructor(nums) {
    this.nums = nums; // array store
  }

  // Number context (like +, -)
  valueOf() {
    return this.nums.reduce((a, b) => a + b, 0);
  }

  // String context
  toString() {
    return `[${this.nums.join(',')}]`;
  }
}
```

### 🧠 ব্যাখ্যা

| ধাপ | কাজ                                 | উদাহরণ                        |
| --- | ----------------------------------- | ----------------------------- |
| 1️⃣ | `constructor(nums)` → nums save করা | `[1,2,3]`                     |
| 2️⃣ | `valueOf()` → number conversion     | `+wrapper` → 6                |
| 3️⃣ | `toString()` → string conversion    | `String(wrapper)` → "[1,2,3]" |

### 🧾 উদাহরণ
```js
const wrap1 = new ArrayWrapper([1,2,3]);
const wrap2 = new ArrayWrapper([4,5,6]);

console.log(+wrap1); // 6
console.log(`${wrap1}`); // "[1,2,3]"
console.log(wrap1 + wrap2); // 6 + 15 = 21

console.log(wrap1.toString()); // "[1,2,3]"
console.log(wrap2.valueOf()); // 15

```

🔹 Key Points
- valueOf() → number context handle করে।
- toString() → string context handle করে।
- JavaScript–এ object operator use করলে valueOf() automatically call হয়।
- Wrapper class দিয়ে array–কে context–specific behavior দেওয়া যায়।


